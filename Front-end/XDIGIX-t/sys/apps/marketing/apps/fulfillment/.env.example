# Backend API URL — must be a full URL with protocol (https://...) so requests go to the API, not the frontend.
# Local:
VITE_API_BACKEND_URL=http://localhost:4000/api
# Railway (example):
# VITE_API_BACKEND_URL=https://xdigix-os-production.up.railway.app/api
# If you set only a hostname (no https://), the app will prepend https:// and /api.

# When backend is on Vercel, socket is auto-disabled (no WebSockets on serverless).
# VITE_AUDIT_SOCKET_ENABLED=false
