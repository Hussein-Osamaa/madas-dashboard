/**
 * Legacy plan schema — re-exports from the authoritative module schema.
 * The legacy interface shape is kept for backward compatibility in legacy routes.
 */
export { Plan, type IPlan } from '../modules/platform-core/plan.schema';
