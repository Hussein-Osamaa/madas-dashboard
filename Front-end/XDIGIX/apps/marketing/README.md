# XDIGIX Marketing / Landing

Customer-facing landing page for XDIGIX. Same UI/UX as the rest of the platform; SEO-first (SSR/SSG recommended).

**Source to extract from:** `sys/apps/marketing/` (Navbar, Hero, Features, HowItWorks, Pricing, Testimonials, CTA, Footer).

**Stack:** React + Vite (or SSR). Uses `@xdigix/design-system`. No Firebase; static or API for contact/CTA only.

**Routes:** Single-page sections (hero, features, how it works, pricing, testimonials, CTA). Optional: /login, /register links to dashboard.
