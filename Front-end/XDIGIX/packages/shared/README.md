# @xdigix/shared

Shared types, API client, and utilities across XDIGIX apps.

**Contents:**
- `src/types/` – Order, Product, Client, User, ScanLog, etc. (align with backend models)
- `src/api/` – Base API client (auth, request), optional endpoint helpers

**Used by:** All apps and packages that call the backend or need common types.
